package lambda.PredicateLambda;

/*

public class PredicateExample2 {
    public static void main(String[] args) {
        List<Person> persons = PersonFactory.createPersons();
        List<Person> filteredList = getFilteredNumber(persons, PersonPredicates.malePersonPredicate);
        filteredList.forEach(System.out::println);
        }




    public static List<Person> getFilteredNumber(List<Person> persons, Predicate<Person> personPredicate){
        filteredPersons = new ArrayList<Person>();
        for(Person p :persons){
            if (personPredicate.test(p)) {
                filteredPersons.add(p);
            }
        }
        return filteredPersons;
    }
}

public class PredicatePersons{
    public static Predicate<Persons> malePersonPredicate = p -> p.getGender().equals(Gender.MALE);

}
*/